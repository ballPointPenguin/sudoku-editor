const StatusBar = ({ status }) => (
  <div className="flex justify-center items-center mt-5 text-lg h-10 w-full max-w-[80vmin] bg-gray-100 rounded-md px-2.5">
    {status}
  </div>
)

export default StatusBar
